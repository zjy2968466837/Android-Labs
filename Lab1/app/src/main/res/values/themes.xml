<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- 主题基类：Material3 的 DayNight 变体，浅色/深色由系统自动选择。
         标题栏不使用系统自带的 ActionBar，而是在 MainActivity 里用纯代码创建的 Toolbar。

         状态栏/导航栏不留白也不单独指定颜色：界面采用 edge-to-edge 绘制
         （Android 15 起 targetSdk 35+ 强制），系统栏区域由根布局背景透出，
         具体留白在 MainActivity 的 WindowInsets 监听器里处理。 -->
    <style name="Base.Theme.CourseDemo" parent="Theme.Material3.DayNight.NoActionBar" />

    <style name="Theme.CourseDemo" parent="Base.Theme.CourseDemo" />
</resources>
